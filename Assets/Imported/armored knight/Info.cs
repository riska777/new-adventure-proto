using UnityEngine;
using System.Collections;

public class Info : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}

    // Thanks for purchasing this asset.

    // The animations used in the test scene belong to the package: Raw mocap data for mecanim, at the asset store. the two animations are located in the animation folder.

    //The model is not animated, but it will be in a future update of the package, including facial animation.

    //For any doubt, problem or suggestion, please don´t hesitate to contact me at amarok.pelo@gmail.com
}
